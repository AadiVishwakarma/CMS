spring:
  datasource:
    url: jdbc:mysql://localhost:3306/zee5contentdashboard
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: true
    hibernate:
#      naming:
#        physical-strategy: com.learning.utils.CustomNamingStrategy
      ddl-auto: update
  jackson:
    serialization:
      fail-on-empty-beans: false
      
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
server:
  port: 9005